# 活动洞察视觉验收

状态：**布局与交互通过**
验收日期：2026-08-25
参考图：`codex-clipboard-766d9aae-1c72-4f6b-a003-92d2af0d07f3.png`
真实应用：`dist/Codex Account Switch.app`，默认窗口 860×720

## 已检查

- 使用真实 Codex 账户数据加载累计 Token、峰值日、最长会话、当前/最长连续天数。
- 每日 52 周热力图、每周柱状图和累计增长图均可切换；三个标签均暴露为无障碍按钮。
- 快速模式、常用推理强度、技能统计、聊天总数和前五项插件/技能完整显示。
- 在默认窗口中没有文字裁切、卡片重叠、按钮缺字或底部内容溢出。
- 最长会话使用小时/分钟格式；累计趋势按当前可见区间起点使用线性尺度。
- 参考图与应用截图完成同宽上下拼接比对；信息层级和布局关系一致，同时保留项目既有深色控制台视觉。
- 活动接口返回可空插件字段时可以解析；临时失败时保留上次成功快照。

## 有意保留的差异

- 参考图为浅色 Web 页面；应用沿用现有深色原生桌面主题、顶部账号切换和底部状态栏。
- 接口不提供插件图标资源，界面使用明确的 `PLUGIN`/`SKILL` 类型徽标，不伪造图标资产。
- 月份标签未复制到紧凑窗口；页面以“过去 52 周”和日期密度表达范围，优先保证 780px 最小宽度可用。

## 交付说明

- 最终 macOS Release 位于 `dist/Codex Account Switch.app`；安装到 `/Applications` 后应再次核对主二进制 SHA-256。
- 最终一次自动重启截图受 macOS 锁屏阻挡；锁屏前已在安装版完成每日/每周/累计状态和返回操作的真实窗口检查，最后一次改动仅调整累计数值归一化，并由单元测试覆盖。
