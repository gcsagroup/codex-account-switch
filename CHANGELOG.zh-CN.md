# 变更记录

[English](CHANGELOG.md) | [简体中文](CHANGELOG.zh-CN.md) | [繁體中文](CHANGELOG.zh-TW.md)

本文档记录用户可见功能、兼容性、安全性和交付变化。项目目前没有正式 Release，因此当前实现统一归入 `0.1.0`。

## 0.1.0 - 未发布

### 新增

- Codex 与 Claude Desktop 本地 Profile 的保存、切换、改名、可恢复删除和恢复。
- 切换前恢复快照、事务回滚、私有回收区和“恢复上次删除”。
- Codex 5 小时/每周额度、百分比、更新/重置时间、credits 和各账号状态。
- 累计/峰值 Token、当前/最长连续天数、最长会话、近期活动和 52 周活动洞察。
- 浏览器授权、保存当前、路径导入、文本粘贴和剪贴板导入。
- 托盘显示窗口、刷新额度、切换账号和退出操作。
- 英语、简体中文、繁体中文、日语、韩语、法语和西班牙语界面。

### 改进

- 产品和交付文件统一为 `Codex Account Switch` / `codex-account-switch`；保留 `CODEX_SWITCH_HOME` 与 `~/.codex-switch` 以兼容已有数据。
- “关于”窗口改为“GCSA 内部使用小工具”，补充产品简介与 Slint 归属信息。
- 重构账号卡片、额度/重置显示、详情预览、导航、导入面板和状态层级。
- 正确归类只返回每周窗口的 Pro 响应，并避免继续显示已经过期的额度窗口。
- 增加额度刷新 single-flight、当前账号每分钟刷新和完整账号周期刷新。
- 活动接口单独失败时保留上次成功的活动快照。
- Claude Desktop Cookies 改用 SQLite Backup API，并检查完整性与 sidecar。
- macOS/Windows 构建脚本支持外部 `CARGO_TARGET_DIR`。

### 修复与加固

- 防止浏览器登录或导入账号覆盖身份不同的已保存 Profile。
- Codex 切换增加目标复验、身份比较、token 写回、恢复快照、回滚和私有文件权限。
- Claude 切换增加 SQLite 完整性检查、文件锁、恢复快照、sidecar 处理和回滚。
- 修复账号卡片裁切、按钮文字缺失、默认窗口溢出、语言菜单遮挡及 macOS 关闭窗口但进程不退出。
- 明确标记损坏 Profile，并禁用其“切换”和“重启”。
- 增加单实例锁、额度刷新串行化、登录取消和 300 秒登录超时。
- 补齐七种界面语言，并增加翻译完整性测试。

### 构建与验证

- 固定 Rust `1.97.1`；CI 在 macOS/Windows 运行格式、Clippy、测试和 Release 构建。
- 增加 macOS 打包、签名和公证脚本。
- 增加 Windows x64/x86 构建、SHA-256 文件和 x64 兼容别名。
- 当前自动化基线：32 项测试，覆盖凭据、导入、权限、恢复、切换、预览、Claude SQLite、额度窗口和活动解析。
