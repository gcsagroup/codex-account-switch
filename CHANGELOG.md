# 变更记录

本文档记录 Codex Account Switch 的用户可见功能、兼容性、安全性和交付变化。项目目前尚无已发布版本，因此当前全部实现统一归入 `0.1.0`。

## 0.1.0 - 未发布

### 新增

- 支持 Codex 与 Claude Desktop 本地账号档案的保存、切换、改名、删除及恢复。
- 增加切换前恢复快照、失败回滚、私有回收区和“恢复上次删除”。
- 为每个 Codex 账号分别展示 5 小时/每周额度、使用率、刷新时间及额度重置时间。
- 展示当前 Codex 账号的累计 Token、峰值日、连续使用、最长会话和最近七天用量趋势。
- 增加当前 Codex 账号“活动洞察”页，提供 52 周每日热力图、每周/累计趋势、当前与最长连续天数、快速模式、常用推理强度、技能/聊天统计和常用插件/技能排行。
- 增加浏览器授权登录、文件路径导入、剪贴板与文本导入入口。
- 浏览器授权支持取消、超时和登录后自动保存，默认按邮箱前缀生成不冲突的别名。
- 增加系统托盘的显示窗口、刷新额度、快速切换和退出操作。
- 增加关于入口、状态提示以及简体中文、繁体中文、英语、日语、韩语、法语和西班牙语界面。

### 改进

- 产品名称统一为 `Codex Account Switch`，包名和交付文件统一为 `codex-account-switch`；为兼容已有安装，继续使用 `CODEX_SWITCH_HOME` 和 `~/.codex-switch` 数据目录。
- 重做“关于”弹窗：标题改为“GCSA 内部使用小工具”，补充本地账号切换、额度与活动洞察简介；保留 Slint 许可要求的官方归属组件，并随 macOS/Windows 分发物提供第三方许可证和源码 URL 说明。
- 重做账号卡片的额度信息行：重置时间在左侧，窗口名称与百分比在右侧紧凑显示；点击账号卡片可切换左侧详情预览，不会误触发实际账号切换。
- 重构深色界面的顶部导航、当前账号面板、额度卡片、导入面板和账号列表，统一颜色、间距和按钮层级。
- 账号列表使用独立状态卡片，高额度账号突出显示，并在列表内直接显示各账号的重置时间。
- 正确识别仅返回每周窗口的 Codex Pro 额度响应，不再误显示为 5 小时窗口。
- 额度窗口过期后显示等待刷新状态，避免继续展示旧使用率。
- 当前账号每分钟后台刷新，完整账号列表按需刷新；额度刷新使用 single-flight 避免重复请求。
- 活动趋势使用对数归一化保留低用量日期的可见性，累计趋势以当前可见区间起点展示增长。
- macOS/Windows 构建脚本支持复用 `CARGO_TARGET_DIR`，便于在项目目录外保存构建缓存。
- 切换 Codex 账号前写回当前账号刷新后的 token，切换后可选择自动重启 ChatGPT/Codex 桌面端。
- 切换 Claude Desktop 账号时自动退出、替换一致性 Cookies 快照并重新启动 Claude Desktop。
- 使用 macOS `AppleLocale` 识别简体中文区域，并持久化用户选择的界面语言。
- Claude Desktop 会话改用 SQLite Backup API，并检查数据库完整性与 sidecar 状态。

### 修复

- 修复 Slint 嵌套布局将账号名、套餐、邮箱、更新时间和按钮文字压缩或裁切的问题。
- 修复默认窗口高度下三个 Profile 显示不完整、控件重叠及语言菜单遮挡问题。
- 修复 macOS 点击标题栏关闭按钮后进程仍驻留的问题；标题栏关闭、界面“退出”和托盘退出现在统一结束应用。
- 加固 Codex 登录、导入和账号切换的一致性，拒绝空、损坏或账号不匹配的凭据。
- 修复外部登录后 `current` 未同步、后续保存可能覆盖旧账号的问题。
- 修复 live 凭据与当前 Profile 身份不一致时仍可能覆盖旧 Profile 的问题。
- 修复更新当前标记失败后 live 凭据无法回滚的问题。
- 无效 Profile 现在明确标记并禁止切换或重启，不再与正常账号表现一致。
- Claude Desktop Cookies 保存与切换增加 SQLite `quick_check`、表结构验证、文件锁和失败回滚。
- 临时文件在创建时即使用私有权限，并增加单实例与额度刷新单飞保护。
- 补齐所有界面语言的 Claude 文案和状态文案，并增加翻译完整性测试。
- 修复活动接口返回可空插件/技能字段时整份数据解析失败的问题；活动接口临时失败时保留上次成功洞察。

### 构建与交付

- 固定 Rust 1.97.1 工具链，并增加格式检查、测试和 Clippy CI。
- 使用 Rust、Slint 1.17、rustls 和 bundled SQLite 构建原生桌面应用，不依赖 WebView。
- 增加 macOS App 打包、签名与公证脚本。
- 同时生成 Windows x64 与 Windows x86 可执行文件及 SHA-256 校验文件；`codex-account-switch.exe` 保留为 x64 兼容别名。
- 当前自动化测试覆盖身份解析、导入、权限、回收恢复、事务切换、详情预览、Claude SQLite、额度窗口和活动洞察解析，共 32 项。
